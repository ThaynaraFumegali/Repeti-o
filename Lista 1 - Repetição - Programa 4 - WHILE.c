#include <stdio.h>
#include <stdlib.h>

int main()
{
    int codigo, veiculos, acidentes, i;
    i=1;
    while(i<=5)
    {
      printf("Digite o codigo da cidade: ");
      scanf("%d", &codigo);
      printf("Qual o numero de veiculos de passeio nessa cidade? ");
      scanf("%d", &veiculos);
      printf("Qual o numerod e acidentes de transiito com vitmas? ");
      scanf("%d", &acidentes);
      i=(i+1);
      if acidentes>1
      {
        acidentes1=acidentes;
        if acidentes1   
      }
       
      veiculos=veiculos+veiculos
    }
    return 0;
}
